---
title: HTTP / HTTPS 面试回答
description: 面试中如何回答 HTTP 和 HTTPS——30 秒速答 + 2 分钟详解 + 追问预判
category: 面试回答
type: interview
score: 0
difficulty: 中级
frequency: ⭐⭐⭐⭐⭐
status: filled
created: 2026-07-06
updated: 2026-07-10
reviewed: null
tags:
  - HTTP
  - HTTPS
  - TLS
  - 状态码
  - 面试回答
---

# HTTP / HTTPS 面试回答

## Q1: HTTP 和 HTTPS 有什么区别？

### 30 秒版本

"HTTPS = HTTP + TLS 加密层。四个区别：HTTP 明文、HTTPS 加密；HTTP 80 端口、HTTPS 443；HTTPS 需要 CA 证书；HTTPS 防篡改和防窃听。"

### 2 分钟版本

"从用户访问 HTTPS 网址说起，浏览器和服务器做了三件事：

**证书验证**：服务器发来 SSL 证书——含公钥和 CA 签名。浏览器用内置根证书逐级验证证书链——根 CA→中间 CA→网站证书。任何一环不信任就报证书错误。

**密钥交换**：主流是 ECDHE 协商——服务端把自己的 ECDHE 参数用证书私钥**签名**后发来，浏览器验签后回传自己的 ECDHE 公钥，双方各自算出相同的会话密钥。密钥本身从不在网络上传输，而且有前向安全（私钥泄露也解不开历史流量）。老式的 RSA 密钥交换（浏览器生成对称密钥、用服务器公钥加密发过去）已经过时——TLS 1.3 已将其移除。

**对称加密通信**：之后全用会话密钥对称加密（AES）——快且安全。

**为什么不全用非对称加密？** 非对称比对称慢 100-1000 倍。非对称/DH 协商只负责建立会话密钥，大流量数据全走对称加密，是安全和性能的平衡。

**TLS 1.3 改进**：握手 2-RTT→1-RTT，去掉不安全加密算法，默认前向安全性。"

### 追问预判

| 面试官追问 | 你的回答 |
|-----------|---------|
| "中间人攻击怎么防" | HTTPS 证书链验证——攻击者没有 CA 私钥，无法伪造合法证书。前提是用户没手动信任不可信证书 |
| "HTTP2 相比 1.1 的改进" | 多路复用（一个连接并发多请求）、头部压缩（HPACK）、服务器推送、二进制分帧。最关键的是多路复用——解决 HTTP1.1 队头阻塞 |
| "HTTP 状态码分类" | 1xx 信息、2xx 成功(200/201/204)、3xx 重定向(301永久/302临时/307/308)、4xx 客户端错误(400/401未认证/403无权限/404/405)、5xx 服务端(500/502网关/504超时)。注意：304 是协商缓存状态码，属于 3xx 但不属于"重定向"——它告诉浏览器"用本地缓存，不用重新下载" |

## 别踩的坑

1. **"HTTPS = HTTP + SSL"太笼统** —— 面试官想听 TLS 握手和证书链。至少说清"证书负责身份认证（私钥只做签名），会话密钥靠 ECDHE 协商出来——不是用公钥加密传密钥"
2. **301 和 302 说反** —— 301 永久重定向（浏览器缓存，下次直接跳），302 临时（不缓存）
3. **304 和强缓存混为一谈** —— 304 是协商缓存的结果，有请求往返；强缓存无请求

## 相关阅读

- [HTTP / HTTPS 知识文档](../../网络/http-https.md)
- [HTTP2 / HTTP3](../../网络/http2-http3.md)
- [CORS 面试回答](./cors.md)

## 更新记录

- 2026-07-18：Phase 4 对齐——密钥交换从"公钥加密传对称密钥（RSA 交换）"改为 ECDHE 协商（证书私钥只做签名；RSA 交换已废弃、TLS 1.3 移除），与知识文件 Phase 3 修正一致
- 2026-07-10：重写（30秒/2分钟/追问预判/易错点 标准格式）
