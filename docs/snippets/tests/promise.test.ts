/**
 * Promise 手写实现测试
 * @see ../../手写题/promise.md
 *
 * Phase 2 填充完整测试
 */
import { describe, it, expect } from 'vitest'

describe('MyPromise', () => {
  it.todo('should resolve with a value')
  it.todo('should support chaining')
  it.todo('should catch errors')
})
