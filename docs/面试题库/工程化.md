---
title: 工程化 高频面试题
category: 面试题库
type: interview
score: 0
difficulty: 中级
status: reviewed
tags:
  - 工程化
  - Vite
  - Webpack
  - 构建工具
  - 模块化


---
# 工程化 高频面试题

> 收录前端面试中的高频工程化真题，共 16 题。
> 题目大致按出现频率从高到低排列，新增真题追加在末尾。

---

### Q1: Vite vs Webpack 全面对比

> 🏷️ 对比题
> ⭐⭐⭐⭐⭐ | 难度：中级

**题目**：请详细对比 Vite 和 Webpack 的区别。Vite 为什么比 Webpack 快？Vite 在开发环境和生产环境分别用的是什么构建工具？

**考察点**：
- Webpack 先打包再启动，Vite 利用浏览器原生 ESM 按需编译
- Vite 开发环境用 esbuild 预构建依赖（快 10-100 倍），生产环境用 Rollup 打包
- esbuild 用 Go 编写，多线程并行
- Vite 冷启动快：只编译当前页面需要的模块
- HMR 速度：Vite 只需精确更新变更模块，Webpack 需要重新打包变更模块链
- Webpack 生态更成熟（loader/plugin），Vite 兼容 Rollup 插件生态、新项目默认选 Vite

**30秒答**：Vite 开发用浏览器原生 ESM——不打包按需编译，冷启动秒级。Webpack 先打包全量依赖再启动，大项目几十秒。Vite 生产用 Rollup 打包。HMR Vite 毫秒级——只替换变更模块。
**追问预测**：
- "Vite 生产构建为什么不用 esbuild" → esbuild 打包功能不完整——代码分割和 CSS 处理有局限。用 Rollup
- "Vite 的预构建做了什么" → CJS→ESM 转换、多小文件合并减少请求数
- "大项目 Vite 也快吗" → 冷启动依然快——不打包。但首次加载可能发几百个请求
- "Vite 的局限性" → 开发/生产行为不一致（esbuild vs Rollup）、老浏览器需 @vitejs/plugin-legacy、开发首屏请求瀑布

> 答案参考：[../工程化/vite.md](../工程化/vite.md)
> 🎤 回答稿：[../面试回答/工程化/vite-webpack.md](../面试回答/工程化/vite-webpack.md)
> 延伸：[../工程化/webpack.md](../工程化/webpack.md)

---

### Q2: esbuild vs Babel

> 🏷️ 对比题
> ⭐⭐⭐⭐ | 难度：中级

**题目**：esbuild 和 Babel 有什么区别？为什么 esbuild 这么快？esbuild 可以作为 Babel 的替代品吗？

**考察点**：
- Babel 用 JS 编写，单线程；esbuild 用 Go 编写，多线程并行
- esbuild 比 Babel 快 10-100 倍
- esbuild 的功能：转译、压缩、打包，但功能不如 Babel 全面
- Babel 生态更丰富（大量 plugin 和 preset）、支持自定义插件
- esbuild 的限制：不支持 AST 级别的自定义转换、部分 ES 新特性支持滞后
- 最佳实践：esbuild 做基础转译 + Rollup 打包，Babel 做复杂的代码转换

**30秒答**：esbuild Go 编写多线程比 Babel(JS 单线程)快 10-100 倍。esbuild 不支持 AST 级自定义转换——不能完全替代 Babel。最佳实践：esbuild 做基础转译，Babel 做复杂代码转换。
**追问预测**：
- "esbuild 比 Babel 快多少" → 10-100 倍——Go 编写多线程 vs JS 单线程
- "esbuild 能替代 Babel 吗" → 不能完全替代——esbuild 不支持 AST 级自定义转换、部分 ES 新特性滞后
- "实际项目怎么用" → esbuild 做基础转译——Babel 做复杂代码转换和自定义 plugin

> 答案参考：[../工程化/babel-esbuild.md](../工程化/babel-esbuild.md)

---

### Q3: Tree Shaking 原理与实践

> 🏷️ 概念题
> ⭐⭐⭐⭐ | 难度：高级

**题目**：Tree Shaking 的实现原理是什么？为什么 Tree Shaking 必须基于 ESM？如何确保项目的 Tree Shaking 生效？

**考察点**：
- Tree Shaking 依赖 ESM 的静态结构（import/export 编译时确定）
- Webpack 方式：标记未使用的 export（usedExports）+ Terser 删除死代码
- sideEffects：`package.json` 中标记，告知打包工具哪些文件有副作用
- `/*#__PURE__*/` 注释标记函数调用无副作用
- 为何不生效：CommonJS 混用、Babel 把 ESM 转成了 CJS、副作用判断失误
- 检查方式：webpack-bundle-analyzer 分析打包结果

**30秒答**：Tree Shaking 依赖 ESM 静态结构——编译时确定 import/export。标记未使用导出+Terser 删除死代码。sideEffects:false 告知打包工具可安全删除。不生效原因：CJS 混用、Babel 转译破坏了 ESM。
**追问预测**：
- "Tree Shaking 为什么必须 ESM" → ESM 编译时静态结构——import/export 在代码最顶层、不能条件动态
- "sideEffects 字段的作用" → 标记文件有副作用——打包工具不会删除。false 表示可安全删除未使用导出
- "Tree Shaking 为什么不生效" → CJS 混用、Babel 转译写死了导入、副作用判断有误

> 答案参考：[../工程化/tree-shaking.md](../工程化/tree-shaking.md)
> 🎤 回答稿：[../面试回答/工程化/tree-shaking-hmr.md](../面试回答/工程化/tree-shaking-hmr.md)

---

### Q4: HMR（热模块替换）原理

> 🏷️ 概念题
> ⭐⭐⭐⭐ | 难度：高级

**题目**：Webpack 和 Vite 的 HMR 原理分别是什么？HMR 相比 Live Reload 有什么优势？如何实现自定义模块的 HMR？

**考察点**：
- HMR 只替换变更模块，保留应用状态；Live Reload 全页面刷新
- Webpack HMR：WebSocket 推送变更模块 hash -> 客户端请求变更模块 -> 模块热替换
- Vite HMR：原生 ESM + WebSocket，直接精确替换变更模块
- `module.hot.accept()` 注册模块更新的回调
- 实现原理：模块工厂函数、module cache 的替换
- 为什么有时 HMR 会失败（刷新页面）：模块依赖链变更、不可热替换的操作

**30秒答**：HMR 替换变更模块保留应用状态——Live Reload 全页面刷新丢状态。Webpack HMR WebSocket 推送变更→请求模块→替换。Vite HMR 原生 ESM 精确替换——不需要重新打包模块链。
**追问预测**：
- "HMR 和 Live Reload 的区别" → HMR 替换模块保留状态；Live Reload 全页面刷新丢失状态
- "Vite HMR 为什么比 Webpack 快" → 原生 ESM——精确替换变更模块，不需要重新打包模块链
- "module.hot.accept 的作用" → 注册模块更新的回调——自定义 HMR 行为

> 答案参考：[../工程化/vite.md](../工程化/vite.md)
> 🎤 回答稿：[../面试回答/工程化/tree-shaking-hmr.md](../面试回答/工程化/tree-shaking-hmr.md)
> 延伸：[../工程化/webpack.md](../工程化/webpack.md)

---

### Q5: pnpm vs npm/yarn

> 🏷️ 对比题
> ⭐⭐⭐⭐ | 难度：中级

**题目**：pnpm 相比 npm 和 yarn 有什么优势？pnpm 的"幽灵依赖"问题是如何解决的？硬链接和符号链接是怎么工作的？

**考察点**：
- npm/yarn 的扁平化 node_modules 导致"幽灵依赖"（可引用未声明的包）
- pnpm 使用内容寻址存储（Content-Addressable Store），所有包全局共享一份
- pnpm 的 node_modules：`.pnpm` 目录 + 符号链接，只暴露 `package.json` 中声明的依赖
- 硬链接节省磁盘空间，依赖安装速度更快
- 严格的依赖访问控制：未声明的依赖无法 require/import

**30秒答**：pnpm 内容寻址存储——全局共享包节省磁盘。严格依赖访问——未声明包无法 import(解决幽灵依赖)。硬链接+符号链接机制。npm/yarn 扁平化可访问未声明依赖。
**追问预测**：
- "pnpm 的幽灵依赖是什么" → npm/yarn 扁平化后可以引用未声明的包——pnpm 直接禁止访问
- "pnpm 怎么节省磁盘" → 内容寻址存储——所有包全局共享一份，硬链接到项目
- "pnpm 的缺点" → 某些包未正确声明依赖可能在 pnpm 下报错——需补 declaration

> 答案参考：[../工程化/pnpm.md](../工程化/pnpm.md)

---

### Q6: Webpack Plugin vs Loader

> 🏷️ 对比题
> ⭐⭐⭐⭐ | 难度：中级

**题目**：Webpack 中 Loader 和 Plugin 有什么区别？Loader 的执行顺序是怎样的？请写出一个自定义 Plugin 的基本结构。

**考察点**：
- Loader：文件转换（将非 JS 文件转为 JS 模块），作用于单个文件
- Plugin：扩展 Webpack 功能，作用于整个构建流程（通过钩子 hooks）
- Loader 执行顺序：从右到左、从下到上（`use: ['style-loader', 'css-loader']`）
- Plugin 基本结构：class 包含 `apply(compiler)` 方法，通过 `compiler.hooks` 挂载
- Tapable：Webpack 的插件系统，同步（SyncHook）和异步（AsyncSeriesHook）钩子

**30秒答**：Loader 文件转换器——单个文件转换。Plugin 构建流程控制——整个生命周期。Loader 从右到左链式执行。Plugin 基于 Tapable 钩子系统——通过 compiler.hooks 注册。
**追问预测**：
- "Loader 和 Plugin 怎么通信" → Loader 通过 this.emitFile 输出文件；Plugin 通过 compilation 对象读取
- "写一个 Plugin 需要什么" → class + apply(compiler) 方法 + 挂载 compiler.hooks
- "Tapable 是什么" → Webpack 插件系统——SyncHook/AsyncSeriesHook 多种钩子类型

> 答案参考：[../工程化/webpack.md](../工程化/webpack.md)
> 🎤 回答稿：[../面试回答/工程化/vite-webpack.md](../面试回答/工程化/vite-webpack.md)

---

### Q7: Source Map 类型与选择

> 🏷️ 概念题
> ⭐⭐⭐ | 难度：中级

**题目**：Source Map 有哪些类型（eval、cheap、module、nosources、inline）？开发环境和生产环境分别应该选择哪种？各自有什么区别？

**考察点**：
- `eval`：最快的构建速度，不生成 `.map` 文件，用 eval 包裹
- `source-map`：最全的信息（行列映射），最慢的构建速度
- `cheap`：只映射到行，忽略列，略快
- `module`：包含 loader 的 source map
- `inline`：source map 内联到文件（Data URL），文件体积大
- 开发推荐：`eval-cheap-module-source-map`
- 生产推荐：`hidden-source-map`（不暴露源码位置）或 `source-map`（监控需要）

**30秒答**：开发 eval-cheap-module-source-map 快。生产 hidden-source-map 不暴露源码。cheap 忽略列减小体积。Source Map 不要部署到公开 CDN——上传 Sentry 用于错误还原。
**追问预测**：
- "开发/生产环境 Source Map 怎么选" → 开发 eval-cheap-module-source-map（快）；生产 hidden-source-map（不暴露源码）
- "Source Map 太大怎么办" → cheap 忽略列信息减小体积——生产用 hidden 不部署到 CDN
- "怎么防止 Source Map 泄露" → hidden-source-map 或上传到 Sentry 不在公开 CDN 上

> 答案参考：[../工程化/webpack.md](../工程化/webpack.md)
> 🎤 回答稿：[../面试回答/工程化/vite-webpack.md](../面试回答/工程化/vite-webpack.md)

---

### Q8: CI/CD 流程

> 🏷️ 概念题
> ⭐⭐⭐ | 难度：中级

**题目**：请描述你的项目使用的 CI/CD 流程。代码合并到主分支后发生了什么？如何保证代码质量和部署安全？

**考察点**：
- CI（持续集成）：代码 push -> lint 检查 -> 单元测试 -> 构建 -> 产物归档
- CD（持续部署）：构建产物 -> 部署到测试/预发环境 -> 自动化/手动测试 -> 生产环境
- 分支策略：feature -> develop -> staging -> master/main
- 代码质量卡点：ESLint / Prettier / commitlint / Husky 钩子
- 部署策略：蓝绿部署、灰度发布（金丝雀）、回滚机制
- Docker + K8s 容器化部署

**30秒答**：CI 持续集成——代码 push→lint→test→build。CD 持续部署——产物部署到测试→预发→生产。前端 CI/CD 关键：lint 检查+单元测试+构建产物验证+自动化部署。
**追问预测**：
- "前端 CI/CD 的关键步骤" → lint→test→build→deploy preview→approval→deploy production
- "怎么保证部署安全" → 蓝绿部署/灰度发布、一键回滚、部署前自动化测试
- "GitHub Actions 和 Jenkins 怎么选" → Actions 简单免费适合开源；Jenkins 灵活功能强适合企业

> 答案参考：[../工程化/vite.md](../工程化/vite.md)
> 🎤 回答稿：[../面试回答/工程化/vite-webpack.md](../面试回答/工程化/vite-webpack.md)

---

### Q9: Monorepo 实践

> 🏷️ 概念题
> ⭐⭐⭐ | 难度：高级

**题目**：什么是 Monorepo？你用过哪些 Monorepo 工具（Turborepo / Lerna / Nx / pnpm workspace）？Monorepo 和 Multirepo 各有什么优缺点？

**考察点**：
- Monorepo：多个项目/包在同一个仓库中管理
- pnpm workspace：native 支持 workspace，结合 `workspace:*` 协议
- TurboRepo：增量构建 + 缓存（智能判断哪些包需要重新构建）
- 优点：代码共享简单、统一版本管理、原子化提交、统一的工具链
- 缺点：仓库体积大、权限管理困难、CI 时间可能变长（需增量构建优化）
- 包之间的依赖拓扑排序构建

**30秒答**：Monorepo 多项目同仓库——代码共享方便、统一工具链、原子化提交。pnpm workspace+workspace:* 协议本地包引用。Turborepo 增量构建只重建有变化的包。缺点：仓库体积大、权限管理难。
**追问预测**：
- "Monorepo 和 Multirepo 怎么选" → Monorepo 代码共享方便/统一工具链；Multirepo 独立发版/权限隔离
- "pnpm workspace 实现原理" → workspace:* 协议引用本地包——自动 symlink
- "Turborepo 的缓存策略" → 增量构建——只重新构建有变化的包和依赖链

> 答案参考：[../工程化/pnpm.md](../工程化/pnpm.md)

---

### Q10: 打包分析与产物优化

> 🏷️ 排查题
> ⭐⭐⭐⭐ | 难度：中级

**题目**：如何分析 Webpack / Vite 的打包产物？如何发现并优化打包体积过大的问题？请说明你实际优化过的案例。

**考察点**：
- webpack-bundle-analyzer / rollup-plugin-visualizer 可视化分析
- 识别重复打包、体积异常的包（如 moment.js 的 locale）
- 代码分割（Code Splitting）：`dynamic import()` + `SplitChunksPlugin`
- `externals` 排除 CDN 资源（如 Vue/ECharts 通过 CDN 引入）
- 按需引入（lodash-es vs lodash，Element Plus 按需导入）
- 压缩：TerserPlugin（JS）/ CssMinimizerPlugin（CSS）/ CompressionPlugin（Gzip/Brotli）

**30秒答**：webpack-bundle-analyzer/rollup-plugin-visualizer 生成 treemap 看各包体积占比。找大包→按需导入/依赖替换/代码分割。对比优化前后首屏 JS 大小和 Lighthouse 分数。
**追问预测**：
- "bundle 分析工具有哪些" → webpack-bundle-analyzer（treemap）/ rollup-plugin-visualizer / source-map-explorer
- "发现重复打包怎么解决" → splitChunks 提取公共依赖——或检查是否版本不一致导致重复
- "怎么量化优化效果" → 对比优化前后的首屏 JS 大小和 Lighthouse 分数

> 答案参考：[../工程化/tree-shaking.md](../工程化/tree-shaking.md)
> 🎤 回答稿：[../面试回答/工程化/build-optimization.md](../面试回答/工程化/build-optimization.md)
> 延伸：[../性能优化/bundle-optimization.md](../性能优化/bundle-optimization.md)

---

### Q11: Webpack 构建速度优化

> 🏷️ 排查题
> ⭐⭐⭐ | 难度：高级

**题目**：Webpack 构建速度慢的问题如何排查和优化？请列出具体的优化手段。

**考察点**：
- speed-measure-webpack-plugin 测量各阶段耗时
- 缩小 Loader 作用范围：`include` / `exclude`
- 缓存：Webpack 5 持久化缓存（`cache: { type: 'filesystem' }`）、babel-loader cacheDirectory
- 多线程：thread-loader / happyPack（Webpack 4）
- noParse 跳过无需解析的库
- resolve 规范：减少 resolve.extensions、使用 resolve.alias
- DllPlugin 预编译不常变化的依赖（Webpack 4，5 中不推荐）

**30秒答**：measure 各阶段耗时→缩小 loader 范围(include/exclude)→Webpack5 持久化缓存(filesystem)→thread-loader 多线程。noParse 跳过无依赖库。resolve.extensions 精简减少文件查找。
**追问预测**：
- "怎么测量构建速度" → speed-measure-webpack-plugin——每个 loader/plugin 耗时一目了然
- "Webpack5 持久化缓存的配置" → cache:{type:filesystem}——二次构建快 60-80%
- "thread-loader 适用场景" → CPU 密集型 loader（babel-loader/ts-loader）——I/O 密集型不要用

> 答案参考：[../工程化/webpack.md](../工程化/webpack.md)
> 🎤 回答稿：[../面试回答/工程化/vite-webpack.md](../面试回答/工程化/vite-webpack.md)

---

### Q12: 模块化方案（ESM / CJS / AMD / UMD）

> 🏷️ 对比题
> ⭐⭐⭐⭐ | 难度：中级

**题目**：请对比 CommonJS 和 ES Module 的区别。`require` 和 `import` 在加载和执行上有什么不同？如何在 Node.js 中使用 ESM？

**考察点**：
- CJS 运行时加载（`require` 可放在条件语句中），ESM 编译时加载（静态结构）
- CJS 输出值的拷贝，ESM 输出值的引用（动态绑定）
- CJS 同步加载，ESM 异步加载（浏览器环境）
- `__dirname`/`__filename` 在 ESM 中不可用，需用 `import.meta.url`
- Node.js 中 `.mjs` 或 `package.json` 中 `"type": "module"`
- `require` 加载 ESM 模块的限制

**30秒答**：CJS 运行时加载/值拷贝/同步——require。ESM 编译时加载/值引用/异步——import。Node .mjs 或 type:module 启用 ESM。ESM 可 import CJS；CJS 不能 require ESM(需动态 import)。
**追问预测**：
- "CJS 和 ESM 的本质区别" → CJS 运行时加载/值拷贝/同步；ESM 编译时加载/值引用/异步
- "Node.js 怎么用 ESM" → .mjs 扩展名或 package.json type:module
- "import 和 require 能混用吗" → ESM 可以 import CJS；CJS 不能 require ESM（需动态 import）

> 答案参考：[../工程化/Node/commonjs-esm.md](../工程化/Node/commonjs-esm.md)
> 🎤 回答稿：[../面试回答/工程化/esm-cjs.md](../面试回答/工程化/esm-cjs.md)

---

### Q13: npm 依赖安全审计与供应链安全

> ⭐⭐ | 难度：中级

**题目**：如何审计 npm 依赖的安全性？`npm audit`、Dependabot、锁文件的作用？如何防止供应链攻击？

**30秒答**：npm audit 扫描已知漏洞、lockfile 锁定版本防篡改、Dependabot 自动提 PR 升级。供应链安全：锁定版本号、审计第三方包源码、避免依赖过多——左手依赖攻击越来越常见。

> 答案参考：[../工程化/npm-deep.md](../工程化/npm-deep.md)

---

### Q14: Vite 依赖预构建

> 🏷️ 概念题
> ⭐⭐⭐⭐⭐ | 难度：中高级

**题目**：Vite 的依赖预构建做了什么？为什么需要预构建？预构建的缓存机制是怎样的，什么时候会失效？

**考察点**：
- 工作流 5 步：启动时扫描入口收集裸模块导入 → esbuild 将 CJS/UMD 转为 ESM → 合并碎片模块（lodash-es 600+ 个内部模块合并成少数几个文件）→ 产物写入 `node_modules/.vite/deps` 缓存 → 重写源码导入路径（`import 'vue'` → `/node_modules/.vite/deps/vue.js?v=hash`）
- 为什么需要预构建：① 浏览器原生 ESM 不认 CJS/UMD 包，必须转换 ② 碎片模块直接加载会触发几百个 HTTP 请求（请求瀑布），合并后大幅减少请求数
- 双层缓存：文件缓存以 lockfile + vite.config 的 hash 为 key，变化才重新预构建；浏览器侧对预构建产物强缓存 `max-age=31536000,immutable`，靠 URL 上的 `?v=hash` 失效
- `optimizeDeps.include` 手动补充扫描不到的依赖（如动态 import 的包）；`exclude` 排除本就是 ESM 且模块少的包
- `vite --force` 强制重新预构建——排查依赖异常的常用手段

**30秒答**：Vite 启动时扫描出所有裸模块导入，用 esbuild 把 CJS 依赖转成 ESM、把碎片模块合并，结果缓存到 node_modules/.vite/deps。两个目的——浏览器只认 ESM、合并请求避免瀑布。缓存 key 是 lockfile 和配置的 hash，没变就直接复用；浏览器侧强缓存 immutable，靠 URL 的 v=hash 更新。依赖异常时 --force 强制重建。
**追问预测**：
- "为什么预构建用 esbuild 而生产构建不用" → 预构建只求快、产物质量要求低——Go 多线程比 JS 快 10-100 倍；生产要成熟的代码分割和 Tree Shaking，用 Rollup
- "预构建缓存什么时候失效" → lockfile、vite.config、依赖版本变化时重建文件缓存；浏览器强缓存靠 ?v=hash 查询参数失效
- "什么时候要手动配 optimizeDeps" → 动态 import 扫描不到的包用 include 补；monorepo 里的本地 ESM 包用 exclude
- "开发正常、生产报错怎么排查" → 开发 esbuild、生产 Rollup 对 CJS 处理方式不同——把问题包加 optimizeDeps.include、检查其导出格式

> 答案参考：[../工程化/vite-deep.md](../工程化/vite-deep.md)
> 延伸：[../工程化/vite.md](../工程化/vite.md)

---

### Q15: 下一代构建工具：Rspack 与 Turbopack

> 🏷️ 概念题
> ⭐⭐⭐⭐ | 难度：中级

**题目**：了解 Rspack 和 Turbopack 吗？为什么新一代构建工具都在用 Rust 重写？新项目和存量项目分别应该怎么选构建工具？

**考察点**：
- 演进逻辑：JS 编写的工具（Webpack/Babel）碰到性能天花板——单线程、JIT 预热、GC 停顿，原生语言成为出路（Go 的 esbuild、Rust 的 SWC/Rspack/Turbopack）
- Rspack：字节跳动开源、Rust 实现，兼容 Webpack 的配置结构和 loader/plugin API——存量 Webpack 项目近乎无痛替换，构建速度约为 Webpack 的 5-10 倍
- Turbopack：Vercel 出品、绑定 Next.js（next dev 默认启用），核心是增量计算引擎——函数级缓存，只重算变更部分，不做通用打包器
- Vite 底层同样在 Rust 化：Rolldown（Rust 版 Rollup）逐步替换 Rollup + esbuild
- 选型判断：新项目默认 Vite；存量 Webpack 项目求提速选 Rspack；Next.js 项目用 Turbopack——不为技术时髦而迁移

**30秒答**：这两个都是 Rust 写的下一代打包器。Rspack 是字节开源的，兼容 Webpack 配置和 loader/plugin API——老项目几乎不改配置就能提速五到十倍。Turbopack 是 Vercel 给 Next.js 做的，靠增量计算引擎加速。我的选型是新项目默认 Vite，存量 Webpack 项目上 Rspack，Next.js 就用 Turbopack。
**追问预测**：
- "为什么都用 Rust 重写" → JS 单线程 + JIT 预热 + GC 停顿的性能天花板——原生语言多线程并行、无 GC 停顿
- "Rspack 怎么做到兼容 Webpack" → 对齐 Webpack 的配置结构和 loader/plugin 钩子协议——大部分社区 loader/plugin 可直接复用
- "Vite 会被取代吗" → Vite 走开发不打包路线，且底层也在 Rust 化（Rolldown）——是同方向演进不是取代

> 答案参考：[../工程化/babel-esbuild.md](../工程化/babel-esbuild.md)
> 延伸：[../工程化/webpack.md](../工程化/webpack.md)

---

### Q16: Webpack 迁移 Vite 实战

> 🏷️ 场景题
> ⭐⭐⭐⭐ | 难度：中高级

**题目**：如果要把一个 Webpack 老项目迁移到 Vite，你会怎么做？环境变量、CJS 依赖、动态 require、devServer 代理分别怎么处理？迁移中会踩哪些坑？

**考察点**：
- 入口调整：index.html 移到项目根目录，`<script type="module" src="/src/main.ts">` 作为入口
- 环境变量：`process.env.XXX` → `import.meta.env.XXX`，且只有 `VITE_` 前缀的变量会暴露给客户端
- CJS 依赖：预构建自动转 ESM，转不动的（深层动态 require、导出格式怪的老包）手动加 `optimizeDeps.include`
- 动态加载：`require(变量路径)` / `require.context` 不可用 → 改 `import.meta.glob()` 批量导入
- devServer 迁移：`devServer.proxy` → `server.proxy`（结构相近、rewrite 写法不同）；`output.publicPath` → `base`
- 常见坑：依赖 Node 全局变量（global/process/Buffer）的包报 undefined——Vite 不带 node polyfill，需 define 注入或 polyfill 插件；webpack 特有 API（require.context、module.hot）需逐个改写

**30秒答**：我会先动入口——index.html 提到根目录、用 script type=module 引 main。然后环境变量 process.env 全换成 import.meta.env、客户端变量加 VITE_ 前缀。动态 require 和 require.context 改成 import.meta.glob。proxy 挪到 server.proxy，publicPath 换成 base。最大的坑是 CJS 老包和 node polyfill——预构建处理不了的加 optimizeDeps.include，缺 process、Buffer 这些全局变量的用 define 或 polyfill 插件补。
**追问预测**：
- "require.context 用什么替代" → import.meta.glob——默认懒加载返回动态 import 函数，eager: true 同步导入
- "迁移后开发正常、构建报错" → 开发 esbuild、生产 Rollup 处理 CJS 方式不同——检查报错包的导出格式、加 optimizeDeps.include
- "老浏览器怎么兼容" → @vitejs/plugin-legacy——产出降级 bundle + 按浏览器差异化加载
- "什么项目不建议迁移" → 深度绑定 webpack 特有 loader/plugin 的项目改造成本高——可先用 Rspack 无痛提速

> 答案参考：[../工程化/vite-deep.md](../工程化/vite-deep.md)
> 延伸：[../工程化/webpack.md](../工程化/webpack.md)

---

