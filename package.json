{
  "name": "interview-notes",
  "version": "0.1.0",
  "description": "前端面试知识库 - 体系化、可维护、面试导向",
  "private": true,
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs && node scripts/postbuild.cjs",
    "build:pages": "vitepress build docs --base /interview-notes/ && node scripts/postbuild.cjs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "mermaid": "^10.9.6",
    "vitepress": "^1.6.3"
  }
}
